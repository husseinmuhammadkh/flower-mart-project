## Packages
zustand | State management for the shopping cart
framer-motion | Smooth page transitions and elegant entry animations

## Notes
The application requires a Right-to-Left (RTL) layout for the Arabic language.
The root layout applies `dir="rtl"` to ensure native HTML RTL support.
Uses Zustand for local cart state management.
